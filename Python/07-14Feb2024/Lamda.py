def double(x):
    return x * 2
print(double(5))

# The lambda function is defined in the Lamda.py file. The lambda function is called in the main.py file. The lambda function is called using the following syntax:
double = lambda x: x * 2
print(double(5))